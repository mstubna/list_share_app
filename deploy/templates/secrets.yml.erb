# Create secrets.yml from OpsWorks JSON. Everything under the 'pwa_secrets' key will be transformed
# into YAML. Format JSON like:
# {
#   "pwa_secrets": {
#     "production": {
#       "aws_access_key": "xxxxxxxxxxxxxxxxx",
#       "aws_secret_access_key": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
#       "secret_key_base": "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
#     }
#   }
# }

# see https://tickets.opscode.com/browse/CHEF-3953 for explanation of to_json
<%= JSON.parse(node[:pwa_secrets].to_json).to_yaml %>
